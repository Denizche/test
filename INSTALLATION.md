# Инструкции по установке и запуску KOMPAS-3D MCP Server

Этот документ содержит пошаговые инструкции по установке и запуску сервера для создания схем деления изделий по ГОСТ 2.701.

---

## 📋 Предварительные требования

Перед установкой убедитесь, что у вас установлены:

### Обязательно

1. **Windows OS** (Windows 7 или выше)
   - Сервер работает только на Windows из-за COM-интерфейса КОМПАС-3D

2. **КОМПАС-3D версия 7.x или выше**
   - Установлен на компьютере
   - Доступен для использования (лицензия активирована)
   - Рекомендуется версия 7.11 или выше

3. **Python 3.7 или выше**
   - Скачать с https://www.python.org/downloads/
   - **Важно**: При установке отметьте "Add Python to PATH"

### Проверка установки

```bash
# Проверка версии Python
python --version

# Проверка версии pip
pip --version
```

---

## 🚀 Пошаговая установка

### Шаг 1: Подготовка директории проекта

```bash
# Перейти в директорию проекта
cd kompas_mcp_server

# Убедитесь, что структура проекта правильная
dir /B server
# Должны быть файлы:
# - main.py
# - models.py
# - kompas_api_handler.py
# - layout_engine.py
# - gost_validator.py
```

### Шаг 2: Установка зависимостей Python

```bash
# Способ 1: Прямая установка из requirements.txt
pip install -r requirements.txt

# Способ 2: Установка с обновлением pip (рекомендуется)
python -m pip install --upgrade pip
pip install -r requirements.txt

# Способ 3: Установка в виртуальное окружение (рекомендуется для разработки)
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

**Что будет установлено:**
- FastAPI 0.104.1
- uvicorn 0.24.0
- Pydantic 2.5.0
- pywin32 306
- python-multipart 0.0.6
- python-dotenv 1.0.0

### Шаг 3: Проверка установки зависимостей

```bash
# Проверка установки FastAPI
python -c "import fastapi; print(fastapi.__version__)"

# Проверка установки Pydantic
python -c "import pydantic; print(pydantic.__version__)"

# Проверка установки pywin32
python -c "import win32com; print('pywin32 установлен')"
```

### Шаг 4: Подготовка директории для выходных файлов

```bash
# Создание директории для сохранения чертежей
mkdir C:\KOMPAS_OUTPUT

# Проверка прав доступа (убедитесь, что у вас есть права на запись)
echo test > C:\KOMPAS_OUTPUT\test.txt
del C:\KOMPAS_OUTPUT\test.txt
```

---

## ▶️ Запуск сервера

### Способ 1: Запуск через main.py (рекомендуется)

```bash
cd server
python main.py
```

**Ожидаемый вывод:**
```
======================================================================
Запуск KOMPAS-3D MCP Server v1.0.0
======================================================================
ОСНОВНАЯ ЦЕЛЬ: Создание схем деления изделий по ГОСТ 2.701
Основной endpoint: POST /api/v1/create_division_scheme
======================================================================
Попытка подключения к КОМПАС-3D...
✓ Успешное подключение к КОМПАС-3D
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Способ 2: Запуск через uvicorn напрямую

```bash
cd server
uvicorn main:app --host 0.0.0.0 --port 8000
```

### Способ 3: Запуск с автоперезагрузкой (для разработки)

```bash
cd server
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### Способ 4: Запуск на другом порту

```bash
cd server
uvicorn main:app --host 0.0.0.0 --port 8001
```

---

## ✅ Проверка работоспособности

### Проверка 1: Проверка статуса сервера

```bash
curl http://localhost:8000/health
```

**Ожидаемый ответ:**
```json
{
  "status": "healthy",
  "kompas_connected": true,
  "version": "1.0.0",
  "message": "Сервер работает корректно, КОМПАС-3D подключен"
}
```

### Проверка 2: Доступ к документации

Откройте в браузере:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Проверка 3: Создание простой схемы деления

Сохраните этот код в файл `test_api.py`:

```python
import requests
import json

url = "http://localhost:8000/api/v1/create_division_scheme"

payload = {
    "product_name": "Тестовое изделие",
    "product_code": "0000.00.00.000",
    "gost_format": "A4",
    "layout_type": "vertical",
    "title_block_data": {
        "designation": "0000.00.00.000",
        "name": "Тестовая схема деления",
        "developer": "Тестовый пользователь"
    },
    "components": [
        {
            "position": 1,
            "name": "Главное изделие",
            "designation": "0000.00.00.000",
            "quantity": 1,
            "level": 0
        },
        {
            "position": 2,
            "name": "Компонент 1",
            "designation": "0000.01.00.000",
            "quantity": 1,
            "level": 1,
            "parent_position": 1
        }
    ],
    "include_bom": True
}

print("Отправка запроса на создание схемы деления...")
response = requests.post(url, json=payload)

if response.status_code == 200:
    result = response.json()
    print(f"\nСтатус: {'✓ Успех' if result['success'] else '✗ Ошибка'}")
    print(f"Сообщение: {result['message']}")
    if result.get('file_path'):
        print(f"Файл сохранен: {result['file_path']}")
else:
    print(f"HTTP Error: {response.status_code}")
    print(response.text)
```

Запустите тест:

```bash
python test_api.py
```

---

## 🔧 Конфигурация

### Переменные окружения

Вы можете использовать переменные окружения для конфигурации:

```bash
# Установка переменных окружения (Windows PowerShell)
$env:KOMPAS_OUTPUT_PATH = "C:\KOMPAS_OUTPUT"
$env:LOG_LEVEL = "INFO"
$env:SERVER_PORT = "8000"

# Запуск сервера
python main.py
```

### Файл .env (опционально)

Создайте файл `.env` в директории `server/`:

```
KOMPAS_OUTPUT_PATH=C:\KOMPAS_OUTPUT
LOG_LEVEL=INFO
SERVER_PORT=8000
SERVER_HOST=0.0.0.0
```

---

## 🐛 Решение проблем при установке

### Проблема 1: "ModuleNotFoundError: No module named 'fastapi'"

**Решение:**
```bash
# Убедитесь, что вы находитесь в правильной директории
cd kompas_mcp_server

# Переустановите зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### Проблема 2: "ModuleNotFoundError: No module named 'win32com'"

**Решение:**
```bash
# Установите pywin32
pip install pywin32

# Если это не помогает, переустановите
pip uninstall pywin32 -y
pip install pywin32==306
```

### Проблема 3: "КОМПАС-3D не подключен"

**Решение:**
1. Убедитесь, что КОМПАС-3D установлен на компьютере
2. Запустите КОМПАС-3D вручную перед запуском сервера
3. Проверьте версию КОМПАС-3D (должна быть 7.x или выше)
4. Проверьте логи в файле `kompas_mcp_server.log`

### Проблема 4: "Port 8000 is already in use"

**Решение:**
```bash
# Используйте другой порт
cd server
uvicorn main:app --host 0.0.0.0 --port 8001
```

### Проблема 5: "Permission denied" при создании файлов

**Решение:**
1. Убедитесь, что директория `C:\KOMPAS_OUTPUT` существует
2. Проверьте права доступа на запись в эту директорию
3. Запустите командную строку от имени администратора
4. Используйте другую директорию через параметр `output_path` в запросе

---

## 📝 Логирование

### Просмотр логов

Логи сохраняются в файл `kompas_mcp_server.log` в директории `server/`.

```bash
# Windows PowerShell - просмотр последних 50 строк
Get-Content server\kompas_mcp_server.log -Tail 50

# Windows PowerShell - отслеживание логов в реальном времени
Get-Content server\kompas_mcp_server.log -Wait
```

### Уровни логирования

- **DEBUG** - Детальная информация для диагностики
- **INFO** - Информационные сообщения (по умолчанию)
- **WARNING** - Предупреждения о потенциальных проблемах
- **ERROR** - Сообщения об ошибках

---

## 🔐 Безопасность

### Рекомендации для production

1. **Используйте виртуальное окружение**:
   ```bash
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
   ```

2. **Используйте production сервер**:
   ```bash
   pip install gunicorn
   gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app
   ```

3. **Ограничьте доступ**:
   - Используйте firewall для ограничения доступа к порту 8000
   - Добавьте аутентификацию (см. README.md)
   - Используйте HTTPS вместо HTTP

4. **Мониторинг**:
   - Регулярно проверяйте логи
   - Настройте алерты на ошибки
   - Мониторьте использование памяти и CPU

---

## 📦 Обновление зависимостей

### Проверка обновлений

```bash
pip list --outdated
```

### Обновление всех зависимостей

```bash
pip install --upgrade -r requirements.txt
```

### Обновление конкретного пакета

```bash
pip install --upgrade fastapi
pip install --upgrade pydantic
```

---

## 🧹 Очистка и переустановка

### Полная переустановка

```bash
# Удаление всех установленных пакетов
pip freeze | xargs pip uninstall -y

# Переустановка зависимостей
pip install -r requirements.txt
```

### Удаление виртуального окружения

```bash
# Деактивация виртуального окружения
deactivate

# Удаление директории venv
rmdir /S /Q venv
```

---

## 📞 Поддержка

Если у вас возникли проблемы при установке:

1. Проверьте, что все предварительные требования установлены
2. Прочитайте раздел "Решение проблем при установке"
3. Проверьте логи в файле `kompas_mcp_server.log`
4. Обратитесь к документации в README.md

---

## ✨ Следующие шаги

После успешной установки и запуска сервера:

1. **Изучите примеры использования**: см. USAGE_EXAMPLES.md
2. **Создайте первую схему деления**: используйте примеры из документации
3. **Интегрируйте с вашей системой**: используйте API endpoints
4. **Настройте мониторинг**: добавьте логирование и алерты

---

**Версия**: 1.0.0  
**Дата**: 2024-01-15  
**Статус**: Production Ready
