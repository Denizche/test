# KOMPAS-3D MCP Server - Создание схем деления

Промежуточный веб-сервис на **Python (FastAPI)** для интеграции **MCP (Model Context Protocol)** с **КОМПАС-3D**, специализированный на **автоматическом создании схем деления изделий по ГОСТ 2.701**.

## 🎯 Основная цель

**Создание схем деления изделий по ГОСТ 2.701** с поддержкой:

- ✅ **Иерархии компонентов** (parent-child отношения)
- ✅ **Автоматического размещения** компонентов (древовидное, вертикальное, горизонтальное)
- ✅ **Генерации спецификации** (BOM - Bill of Materials)
- ✅ **Валидации по ГОСТ 2.701** (проверка обозначений, иерархии, обязательных полей)
- ✅ **Различных форматов листов** (A0-A4)

---

## 📋 Требования к окружению

### Обязательные

- **Операционная система**: Windows (требуется для работы с COM-интерфейсом КОМПАС-3D)
- **КОМПАС-3D**: версия 7.x или выше, установлен и доступен
- **Python**: 3.7 или выше

### Зависимости Python

```bash
pip install -r requirements.txt
```

Основные пакеты:
- **FastAPI** - веб-фреймворк
- **uvicorn** - ASGI сервер
- **Pydantic** - валидация данных
- **pywin32** - интерфейс COM для КОМПАС-3D

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd kompas_mcp_server
pip install -r requirements.txt
```

### 2. Запуск сервера

```bash
cd server
python main.py
```

или с использованием uvicorn напрямую:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 3. Проверка статуса

```bash
curl http://localhost:8000/health
```

Ожидаемый ответ:
```json
{
  "status": "healthy",
  "kompas_connected": true,
  "version": "1.0.0",
  "message": "Сервер работает корректно, КОМПАС-3D подключен"
}
```

### 4. Создание схемы деления

```bash
curl -X POST http://localhost:8000/api/v1/create_division_scheme \
  -H "Content-Type: application/json" \
  -d '{
    "product_name": "Редуктор цилиндрический",
    "product_code": "1234.00.00.000",
    "gost_format": "A3",
    "layout_type": "tree",
    "title_block_data": {
      "designation": "1234.00.00.000",
      "name": "Схема деления",
      "developer": "Иванов И.И."
    },
    "components": [
      {
        "position": 1,
        "name": "Редуктор цилиндрический",
        "designation": "1234.00.00.000",
        "quantity": 1,
        "level": 0
      },
      {
        "position": 2,
        "name": "Корпус",
        "designation": "1234.01.00.000",
        "quantity": 1,
        "level": 1,
        "parent_position": 1
      }
    ],
    "include_bom": true
  }'
```

---

## 📚 Структура проекта

```
kompas_mcp_server/
├── server/                          # Python-код сервера
│   ├── main.py                      # FastAPI приложение (основной файл)
│   ├── models.py                    # Pydantic модели для валидации
│   ├── kompas_api_handler.py        # Обработчик API КОМПАС-3D
│   ├── layout_engine.py             # Движок размещения компонентов
│   └── gost_validator.py            # Валидатор ГОСТ 2.701
├── requirements.txt                 # Зависимости Python
├── README.md                        # Этот файл
├── USAGE_EXAMPLES.md                # Примеры использования API
└── kompas_mcp_server.log            # Логи сервера (создается при запуске)
```

---

## 🔌 API Endpoints

### POST /api/v1/create_division_scheme

**ОСНОВНОЙ endpoint** для создания схем деления изделий.

**Параметры запроса:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|---------|
| `product_name` | string | ✅ | Наименование главного изделия |
| `product_code` | string | ✅ | Обозначение по ГОСТ (XXXX.XX.XX.XXX) |
| `components` | array | ✅ | Список компонентов в составе изделия |
| `title_block_data` | object | ✅ | Данные основной надписи (штампа) |
| `gost_format` | string | ❌ | Формат листа (A0-A4, по умолчанию A3) |
| `orientation` | string | ❌ | Ориентация (portrait/landscape, по умолчанию landscape) |
| `layout_type` | string | ❌ | Тип размещения (tree/vertical/horizontal, по умолчанию tree) |
| `include_bom` | boolean | ❌ | Включить спецификацию (по умолчанию true) |
| `output_path` | string | ❌ | Путь сохранения файла |

**Структура компонента:**

```json
{
  "position": 1,                    // Позиционный номер (уникальный)
  "name": "Корпус",                 // Наименование компонента
  "designation": "1234.01.00.000",  // Обозначение по ГОСТ
  "quantity": 1,                    // Количество
  "level": 0,                       // Уровень иерархии (0 - главное изделие)
  "parent_position": null,          // Позиция родителя (для иерархии)
  "notes": "Примечание"             // Примечания (опционально)
}
```

**Ответ (успех):**

```json
{
  "success": true,
  "file_path": "C:\\KOMPAS_OUTPUT\\DivisionScheme_1234.00.00.000_a1b2c3d4.cdw",
  "message": "Схема деления успешно создана и сохранена",
  "bom_generated": true
}
```

**Ответ (ошибка):**

```json
{
  "success": false,
  "file_path": null,
  "message": "Ошибка валидации",
  "error_details": "Компонент 1: обозначение не соответствует формату ГОСТ"
}
```

---

### GET /health

Проверка статуса сервера и подключения к КОМПАС-3D.

**Ответ:**

```json
{
  "status": "healthy",
  "kompas_connected": true,
  "version": "1.0.0",
  "message": "Сервер работает корректно, КОМПАС-3D подключен"
}
```

---

### GET /api/v1/info

Информация об API и поддерживаемых типах размещения.

**Ответ:**

```json
{
  "api_version": "1.0.0",
  "api_title": "KOMPAS-3D MCP Server - Division Schemes",
  "primary_purpose": "Создание схем деления изделий по ГОСТ 2.701",
  "supported_layout_types": {
    "tree": "Древовидная структура с иерархией (рекомендуется)",
    "vertical": "Вертикальное размещение в столбец",
    "horizontal": "Горизонтальное размещение в строку"
  },
  "supported_formats": ["A0", "A1", "A2", "A3", "A4"],
  "supported_orientations": ["portrait", "landscape"]
}
```

---

## 📖 Документация API

После запуска сервера документация доступна по адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **OpenAPI JSON**: http://localhost:8000/openapi.json

---

## 💡 Примеры использования

Полные примеры использования API находятся в файле **USAGE_EXAMPLES.md**.

### Простой пример на Python

```python
import requests

url = "http://localhost:8000/api/v1/create_division_scheme"

payload = {
    "product_name": "Редуктор",
    "product_code": "1234.00.00.000",
    "gost_format": "A3",
    "layout_type": "tree",
    "title_block_data": {
        "designation": "1234.00.00.000",
        "name": "Схема деления"
    },
    "components": [
        {
            "position": 1,
            "name": "Редуктор",
            "designation": "1234.00.00.000",
            "quantity": 1,
            "level": 0
        },
        {
            "position": 2,
            "name": "Корпус",
            "designation": "1234.01.00.000",
            "quantity": 1,
            "level": 1,
            "parent_position": 1
        }
    ]
}

response = requests.post(url, json=payload)
result = response.json()

if result["success"]:
    print(f"✓ Схема создана: {result['file_path']}")
else:
    print(f"✗ Ошибка: {result['message']}")
```

---

## 🎨 Типы размещения компонентов

### 1. Древовидная структура (tree) - РЕКОМЕНДУЕТСЯ

Компоненты размещаются по уровням иерархии, отражая структуру изделия.

```
        [1. Редуктор]
             |
    +--------+--------+
    |        |        |
  [2]      [3]      [4]
Корпус   Вал вед.  Вал вед.
```

**Когда использовать**: Для иерархических изделий с четкой структурой.

### 2. Вертикальное размещение (vertical)

Компоненты размещаются в столбец один под другим.

```
[1. Редуктор]
[2. Корпус]
[3. Вал ведущий]
[4. Вал ведомый]
```

**Когда использовать**: Для простых изделий с небольшим количеством компонентов.

### 3. Горизонтальное размещение (horizontal)

Компоненты размещаются в строку один рядом с другим.

```
[1]  [2]  [3]  [4]
```

**Когда использовать**: Для изделий с линейной структурой.

---

## ✅ Валидация по ГОСТ 2.701

Сервер автоматически проверяет соответствие следующим требованиям ГОСТ:

1. **Формат обозначения**: XXXX.XX.XX.XXX (четыре группы цифр через точку)
2. **Уникальность позиционных номеров**: Каждый компонент имеет уникальный номер
3. **Иерархия компонентов**: Родитель должен иметь меньший уровень, чем потомок
4. **Главное изделие**: Должно быть ровно одно изделие с level=0
5. **Обязательные поля**: designation и name в основной надписи
6. **Формат листа**: Поддерживаемые форматы A0-A4
7. **Тип размещения**: Поддерживаемые типы tree, vertical, horizontal

**Примеры ошибок валидации:**

```
✗ Обозначение '1234.00.00' не соответствует формату ГОСТ (XXXX.XX.XX.XXX)
✗ Дублирующиеся позиционные номера: [1, 2]
✗ Компонент 3 ссылается на несуществующий родитель 10
✗ Должно быть ровно одно главное изделие (level=0), найдено: 2
```

---

## 🔍 Логирование

Сервер ведет подробное логирование всех операций:

- **Файл логов**: `kompas_mcp_server.log` (в директории сервера)
- **Уровень логирования**: INFO (по умолчанию)
- **Форматы логов**: Консоль + файл

**Пример логов:**

```
2024-01-15 10:30:45 - main - INFO - Запуск KOMPAS-3D MCP Server v1.0.0
2024-01-15 10:30:46 - kompas_api_handler - INFO - KompasAPIHandler инициализирован
2024-01-15 10:30:47 - kompas_api_handler - INFO - Успешное подключение к КОМПАС-3D
2024-01-15 10:31:00 - main - INFO - Получен запрос на создание схемы деления для 'Редуктор'
2024-01-15 10:31:01 - gost_validator - INFO - Запрос валиден по ГОСТ 2.701
2024-01-15 10:31:05 - kompas_api_handler - INFO - Схема деления успешно создана
```

---

## 🐛 Решение проблем

### Проблема: "Не удалось подключиться к КОМПАС-3D"

**Решение:**
1. Убедитесь, что КОМПАС-3D установлен на компьютере
2. Запустите КОМПАС-3D вручную
3. Проверьте, что версия КОМПАС-3D 7.x или выше
4. Перезагрузите сервер

### Проблема: "Ошибка валидации: обозначение не соответствует формату"

**Решение:**
Используйте формат обозначения XXXX.XX.XX.XXX (четыре группы цифр через точку).

**Примеры корректных обозначений:**
- ✅ 1234.00.00.000
- ✅ 5678.01.02.003
- ❌ 1234.00.00 (не хватает последней группы)
- ❌ 1234-00-00-000 (используются дефисы вместо точек)

### Проблема: "Дублирующиеся позиционные номера"

**Решение:**
Убедитесь, что каждый компонент имеет уникальный position (1, 2, 3, ...).

### Проблема: "Компонент ссылается на несуществующий родитель"

**Решение:**
Проверьте, что parent_position указывает на существующий компонент.

**Пример:**
```json
{
  "position": 3,
  "name": "Вал",
  "parent_position": 10  // ❌ Компонент с position=10 не существует
}
```

---

## 📝 Структура файлов Python

### main.py
Основной FastAPI-сервер с endpoints и обработкой ошибок.

### models.py
Pydantic модели для валидации входящих данных:
- `Component` - модель компонента
- `CreateDivisionSchemeRequest` - запрос на создание схемы
- `DrawSchemaResponse` - ответ с результатом
- `TitleBlockData` - данные основной надписи
- `BOMRow` - строка спецификации

### kompas_api_handler.py
Класс `KompasAPIHandler` для управления КОМПАС-3D:
- `connect()` - подключение к КОМПАС-3D
- `create_division_scheme()` - создание схемы деления
- `_draw_division_scheme()` - черчение компонентов
- `_draw_hierarchy_connections()` - рисование связей между компонентами
- `_generate_bom()` - генерация спецификации

### layout_engine.py
Класс `LayoutEngine` для автоматического размещения компонентов:
- `calculate_positions()` - расчет позиций компонентов
- `_layout_tree()` - древовидное размещение
- `_layout_vertical()` - вертикальное размещение
- `_layout_horizontal()` - горизонтальное размещение
- `validate_layout()` - проверка корректности размещения

### gost_validator.py
Класс `GOSTValidator` для валидации по ГОСТ 2.701:
- `validate_request()` - полная валидация запроса
- `_validate_title_block()` - проверка основной надписи
- `_validate_components()` - проверка компонентов
- `_validate_hierarchy()` - проверка иерархии
- `get_validation_report()` - подробный отчет валидации

---

## 🔐 Безопасность

- Все входные данные валидируются через Pydantic
- Используется CORS для разрешения кросс-доменных запросов
- Обработка исключений на всех уровнях
- Логирование всех операций для аудита

---

## 📦 Развертывание в production

### Рекомендации

1. **Используйте production ASGI сервер** (например, Gunicorn):
   ```bash
   pip install gunicorn
   gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app
   ```

2. **Настройте обратный прокси** (nginx):
   ```nginx
   server {
       listen 80;
       server_name kompas-api.example.com;
       
       location / {
           proxy_pass http://localhost:8000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

3. **Используйте переменные окружения** для конфигурации:
   ```bash
   export KOMPAS_OUTPUT_PATH=/mnt/shared/drawings
   export LOG_LEVEL=INFO
   ```

4. **Настройте мониторинг** и логирование

---

## 🤝 Интеграция с MCP

Этот сервер разработан для интеграции с **Model Context Protocol (MCP)**.

MCP позволяет AI-системам (например, Claude) взаимодействовать с КОМПАС-3D через этот API для создания схем деления.

**Пример MCP интеграции:**

```python
# MCP Tool Definition
{
    "name": "create_division_scheme",
    "description": "Создает схему деления изделия по ГОСТ 2.701",
    "inputSchema": {
        "type": "object",
        "properties": {
            "product_name": {"type": "string"},
            "product_code": {"type": "string"},
            "components": {"type": "array"}
        },
        "required": ["product_name", "product_code", "components"]
    }
}
```

---

## 📞 Поддержка

Для вопросов и предложений обратитесь к разработчикам проекта.

---

## 📄 Лицензия

Проект разработан для использования в соответствии с требованиями ГОСТ 2.701.

---

## 🎓 Дополнительные ресурсы

- **ГОСТ 2.701** - Единая система конструкторской документации. Схемы деления изделий
- **Документация КОМПАС-3D API**: https://kompas.ru/
- **FastAPI документация**: https://fastapi.tiangolo.com/
- **Примеры использования**: см. USAGE_EXAMPLES.md

---

**Версия**: 1.0.0  
**Дата**: 2024-01-15  
**Статус**: Production Ready (для Windows с КОМПАС-3D)
